Implement the plan

# 1 Fix
There are tests that are not running.
Fix the tests. 

# 2 Fix
You are a Python programmer with 20 years of experience.
You also know Cobol programming language.

You have developed this Python application that anonymize Cobol code.
After running anonymization, the application should generate a "mappings.json" file that contains the mapping between the original name and the anonymized name.
Anonymization can be run using different schemes, for instance "corporate". The scheme to use can be passed as an argument to the CLI.
However, currently this "mappings.json" file is not generated.
Fix this bug and make sure that there are tests 